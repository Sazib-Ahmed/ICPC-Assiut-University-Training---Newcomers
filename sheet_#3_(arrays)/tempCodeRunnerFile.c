    // for (int i = 0; i < n - 1; i++) {
    //     for (int j = i + 1; i < n; j++) {
    //         if (a[i] > a[j]) {
    //             int temp = a[i];
    //             a[i] = a[j];
    //             a[j] = temp;
    //         }
    //     }
    // }
